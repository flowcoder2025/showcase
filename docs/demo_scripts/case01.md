# Case 01 — 시연 대본 (거래처별 월별 매출 보고서)

## 1분 버전 (고객 미팅 도입부)

**[10초] 페인 제시**
> "AX상사 박과장님이 매월 거래처 30곳 엑셀을 받아서 합치고 피벗 만드는 데 3시간 쓰셨습니다.
> 마감 다가올수록 야근 단골이죠."

**[5초] 실행**
```bash
uv run python -m cases.case01_excel_vendor_report.scenario
```
→ 콘솔에 timer 출력: `before 180m → after Xs (~Y배)`

**[30초] 결과**
- 자동으로 Quick Look 열림
- "거래처별 월별 매출표 + 막대 차트가 한 시트에. 보고서 양식 그대로."
- 시트 차트 보이도록 스크롤

**[15초] 임팩트**
> "5초. 시간만이 아니라 실수도 사라집니다. 그리고 이 모듈은 다음 회사에 가도 컬럼명 매핑만 바꾸면 그대로 돌아갑니다."

## 3분 버전 (메인 시연)
1분 버전 + 다음 추가:
- 입력 폴더 보여주기 (`personas/sample_data/vendors/`) — "월별 파일이 12개"
- 코드 한 화면 (`cases/case01.../scenario.py`) — "실제로 호출하는 건 3줄"
- Q&A: "다른 회사에 적용하면?" → column_map 보여주기

## 5분 버전 (강의)
3분 버전 + before/after 슬라이드 + 다른 회사 적용 사례 1개 (가상)
