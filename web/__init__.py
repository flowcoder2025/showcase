"""Phase 3-Web — Streamlit MVP entry surface.

Imports nothing eagerly; ``streamlit run web/app.py`` is the only supported
entry point. This module exists so mypy can resolve ``web/`` as a package
under the repo's ``explicit_package_bases`` config.
"""
