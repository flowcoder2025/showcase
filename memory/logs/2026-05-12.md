# 2026-05-12 — Phase 3-Pkg T46 완료 (shim 제거 + dogfood + CI matrix), Phase 3-Pkg ✅ 종료

## 세션 시작 — 사용자 "바로 진행하자" (T46 진입)

전일 종료 시점: HEAD `1dae652` (T45.5 docs). 직전 베이스라인 676 passed, source 67 clean, ceiling 103/13. T46 spec (plan v2.1.1 line 1737~) 8 step.

## 13:30 — T46 진행 (8 step 순차)

**Step 1 — shim 제거**:
- `git rm -r core/` + `git rm tests/test_shim_compat.py`
- 사전 grep `^from core|^import core` 0건 재확인 (T45 step 6 검증 동일)

**Step 2 — 회귀 검증**:
- pytest -q : 671 passed, 4 skipped (T45 676 → -5 shim tests, 회귀 0)

**Step 3 — snapshot 재검증**:
- surface 4 test PASS (snapshot baseline 은 shim 제거에 invariant — shim 은 PUBLIC_MODULES 외였음)

**Step 4 — dogfood scaffold**:
- `tests/dogfood/pyproject.toml` (hatchling, `flowcoder-office-tools[ocr,messaging,docgen,ai]` 의존)
- `requires-python = ">=3.11"` (spec `,<3.14` 대신 — T42 정정 패턴 동일, Plan deviation 2)

**Step 5 — FakeBackend + smoke**:
- `fake_backend.py` — `OCRBackend` / `AIBackend` / `MessagingBackend` Protocol 만족 + `cache_identity()` 계약 + `fake_backends()` factory
- `dogfood_smoke.py` — env-isolated import smoke + SECRET_ENV_NAMES 13건 leak guard + Safe/Fake backend 주입 검증 + ScenarioResult 5+1 필드 + serialize_result/as_display sentinel 마스킹 (R1-C1)
- spec example import 부정확 (실 export 와 어긋남 — `read_excel`/`merge_files`/`build_pivot`/`write_workbook` / `send_discord_with_level` / `extract_receipt` / `chat,draft_email` 모두 부정확) → T45 채택한 sub-module export 패턴 기준으로 정정. Plan deviation 1.

**Step 6 — local 실행 (env -i wrapping)**:
- 첫 시도: `ModuleNotFoundError: No module named 'lxml'` — docgen/hwpx.py 가 lxml 의존이지만 `packages/.../pyproject.toml` 의 `docgen` extras 에 누락. dev workspace install 은 root `lxml>=5.0` dep 로 통과했지만 외부 consumer 시뮬레이션이 fresh install 시 catch.
- `packages/.../pyproject.toml` 의 `docgen` extras 에 `lxml>=4.9` 추가
- fresh venv (Python 3.11) + extras install + dogfood install + `env -i PATH="$PATH" python -m dogfood_smoke` → **`dogfood smoke OK`**

**Step 7 — CI yml**:
- `.github/workflows/ci.yml` — macos-latest × Python 3.11/3.12/3.13 matrix
- 3.11 full pytest + ruff + mypy strict, 매트릭스 전체에서 `env -i PATH=$PATH PYTHONPATH=""` dogfood smoke
- Plan deviation 3: spec `[ocr] extras 3.11 only` 분기 제거 (`ocr` extras = jsonschema + Pillow = pure Python, mlx_vlm 은 subprocess)
- Plan deviation 4: spec `mypy --strict ... web/` 명령에서 `web/` 제외 (T47 진입 시 복원)

**Step 8 — 검증 + commit 전 catch**:
- 첫 pytest 실행: 2 failed (`test_ruff_check_clean` + `test_tests_strict_debt_does_not_grow`)
  - 원인: `tests/dogfood/dogfood_smoke.py` 의 (a) ruff I001 (import 순서) + (b) mypy `tests/` strict 에 새 파일 1 error 추가 (`fake_backend` import-not-found)
- 해결:
  - `ruff check . --fix` (I001 자동 정렬)
  - `pyproject.toml [tool.mypy]` 에 `exclude = ["tests/dogfood/"]` 추가 — dogfood 는 별도 build package 라 main test corpus ceiling scope 외 (lesson 추가)
- 최종 재검증: 671 passed, mypy source 67 clean, mypy tests/ 103/13 ceiling 정확 유지, ruff clean

## 14:00 — T46 commit + push

**커밋**: `c327ab9` WI-T46-feat(pkg): shim 제거 + tests/dogfood/ FakeBackend smoke + CI matrix env -i
- 9 files changed, 290 insertions(+), 209 deletions(-)
- D core/__init__.py, D tests/test_shim_compat.py
- A .github/workflows/ci.yml, A tests/dogfood/{pyproject.toml,fake_backend.py,dogfood_smoke.py}
- M packages/.../pyproject.toml (lxml), M root pyproject.toml (mypy exclude), M uv.lock
- origin/main pushed (`1dae652..c327ab9`)

**Phase 3-Pkg ✅ 완전 종료**:
- T42 ✅ scaffold
- T43 ✅ 이주 + libcst codemod
- T43.5 ✅ py.typed marker
- T44 ✅ shim 안정화 (meta path finder)
- T45 ✅ __all__ + signature snapshot + underscore 격리
- T46 ✅ shim 제거 + dogfood + CI matrix

**critical-gaps §2 해결**: dogfood fixture CI 활성화 완료 (영구 PR merge 차단 게이트로 동작 — local 통과, CI 첫 실행은 main push 와 함께 트리거).

## 14:30 — 세션 종료 (예정)

**최종 baseline (T46 종료)**:
- pytest: 671 passed, 4 skipped (Phase 2 539 → +132 누적)
- mypy --strict packages/.../src + runner + cases: 67 source files clean
- mypy --strict tests/: 103/13 ceiling 정확 유지
- ruff: clean
- 시연: 10/10
- HEAD: `c327ab9` (origin/main 동기화)
- CI matrix: macos-latest × Python 3.11/3.12/3.13 활성화

**핵심 교훈 (lessons.md)**:
- dogfood smoke 가 extras 누락 (lxml) 을 즉시 catch — workspace install 의 dep 그래프 ≠ extras 별 dep 그래프 (서로 다른 검증 도메인). dogfood 는 extras 그래프 검증의 영구 게이트.
- tests/ 하위지만 별도 build package 는 `[tool.mypy] exclude` 로 main ceiling scope 분리 — 외부 consumer 시뮬레이션의 type 검증 의도가 main corpus 와 다르므로 scope 분리가 정직.

**다음 세션 첫 명령** (Phase 3-Web T47 진입):
```bash
cd /Users/jerome/AX/showcase && claude
/mem-resume
git log --oneline -10                          # HEAD c327ab9 (T46) 확인
uv run pytest -q                               # 671 passed 재확인
grep -n "^### T47" specs/2026-05-08-phase3-plan-v2.md  # T47 spec (line 1970~)
```

**T47 핵심**: `web/app.py` 골격 + `.streamlit/config.toml` + 127.0.0.1 default. web/ 디렉토리 신규 + Streamlit MVP entry point. 본 commit 진입 시 CI yml `mypy --strict ... web/` 복원 (T46 deviation 4 정합).

---

## 15:00~ — Phase 3-Web T47~T48.3 진행 (사용자 "진행 고", 7 commits + push)

**오후 세션 누적**:
- `77464a5` **T47** feat(web) — Streamlit MVP 골격 (`.streamlit/config.toml` 127.0.0.1 + `web/app.py` assert + 10 case 카드 + safe-mode toggle). streamlit 1.57.0 root dep + CI yml `web/` 복원
- `3b187e9` **T47.1** fix(web) — button label markdown hard line break (`  \n`) 카드 viewport-width 무관 2줄 일관 (사용자 스크린샷 catch)
- `fee9c97` **T48** feat(web) — `web/_runs.py` (4 primitive: secrets.token_urlsafe + is_relative_to traversal + streaming cap + lock TTL) + `web/_inputs.py` (4 schema 분기) + `web/app.py` execute_case TypedDict + `tests/test_runs_dir.py` 7 TDD test
- `1c8d5c4` **T48.1** fix(web) — `streamlit run web/app.py` 호환 (sys.path[0]=web/ 만 주입 → repo root 보강 + E402 noqa)
- `730ace4` **T48.2** feat(ai) — OpenAI 폴백 dual provider (사용자 요청: "키값 OpenAI 로 대체 → Dual 지원"). client `_resolve_provider()` + `OPENAI_MODEL_PRIORITY` + factory `_ai_api_key()` + runner preflight + dogfood SECRET_ENV_NAMES + .env.example. MODEL_PRIORITY backward compat 유지.
- `70327dd` **T48.3** fix(mlx) — manual shutdown 재invocable + Streamlit sidebar 메모리 컨트롤

**사건 (T48.3 trigger)**:
- 사용자가 활동 모니터에서 90GB+ LLM 메모리 점유 포착 → 맥북 freeze 위험으로 강제 종료
- ps 확인: `mlx_vlm.server PID 42294 (gemma-4-e4b-mlx, port 11438, 10:02 AM 부터)` 살아있음 → SIGTERM 즉시 종료
- 본 세션 직접 LLM 띄움 0건. 추정 trigger: 사용자가 case07/08 OCR 카드를 클릭+실행 시 `_mlx_server.ensure_running` 자동 spawn 됐을 가능성 (또는 이전 세션 잔존). weight-warm 정책 (T28) 이 long-running Streamlit 에서 누적 위험.
- 책임 정리: Streamlit 백그라운드 띄울 때 OCR 자동 spawn 메모리 위험을 사전 안내했어야 함 (사용자에게 명시 사과 + commit body disclose).

**T48.3 변경 (재발 방지)**:
- `_CLEANUP_DONE` 영구 flag 제거 — manual shutdown 후 재spawn 시나리오에서 silent no-op 버그 차단. idempotency 기준을 `_PROCS` 비어있음으로 전환.
- 종료 후 `_PROCS.clear()` — 다음 ensure_running 호출이 fresh spawn 으로 동작.
- `list_running() -> dict[alias, {port, pid}]` 신규 — UI snapshot surface.
- Streamlit sidebar `MLX OCR 서버 (메모리 관리)` expander — 활성 server 표시 + "MLX 서버 종료" 버튼 (running 중 disabled, R1-M4 동일 정책) + 클릭 시 `shutdown_all()` + `st.rerun()`.
- 테스트 fixture `_CLEANUP_DONE` 참조 제거 (변수 자체 사라짐). ceiling 103/13 정확 복귀.

**T48.2 사용자 요청 처리 (AskUserQuestion)**:
- 옵션: 완전 대체 / Key env 이름만 / **Dual 지원** ← 사용자 선택
- 결정: `OPENROUTER_API_KEY` 우선, 부재 시 `OPENAI_API_KEY` 폴백, 둘 다 부재 시 `force_safe`. MODEL_PRIORITY 이름·내용 보존 (Phase 2 DoD test "MODEL_PRIORITY drift" 통과). OPENAI_MODEL_PRIORITY 신규 (gpt-4o-mini, gpt-4.1-mini).
- 효과: 사용자가 `.env` 에 OPENAI_API_KEY 만 추가하면 case09/10 라이브 시연 가능.

**오늘 누적 검증 (T48.3 종료, HEAD `70327dd`)**:
- pytest: **678 passed, 4 skipped** (Phase 2 539 → +139 누적, 회귀 0)
- mypy --strict source: **71 source files clean** (`packages/.../src + runner + cases + web`)
- mypy --strict tests/: **103 errors / 13 files** (T41.5 ceiling 정확 유지)
- ruff: clean
- 시연: 10/10 (case07/08 e2e + T48.3 메모리 통제)
- CI: macos-latest × Python 3.11/3.12/3.13 matrix + dogfood smoke env -i isolated
- 사용자 시각 확인: 카드 정렬 통과, form 분기 통과, AI dual provider 동작 미검증 (다음 시연 시)

**오늘 전체 누적 (T46~T48.3, 8 commits + memory 갱신)**:
- T46 ✅ (Phase 3-Pkg 종료) / T46.5 docs
- T47 ✅ / T47.1 (label fix)
- T48 ✅ / T48.1 (sys.path) / T48.2 (dual provider) / T48.3 (mlx hotfix)

**핵심 교훈 (lessons.md 추가)**:
- long-running UI 의 subprocess weight-warm 정책은 사용자 통제 surface 필수 — atexit 만으로는 부족 (process 종료 시점이 reload 시점이 아님). `idempotency flag = state 자체` 가 영구 boolean 보다 robust.
- T45 surface lock 후 모든 후속 spec example 의 import 가 stale — 핵심 surface 변경 직후 dependent spec example 일괄 sweep 필요 (현재는 task 별 fix 누적).

**Cleanup 권장 (다음 세션 진입 전)**:
- Streamlit 백그라운드 (background ID `bj9h0lcc1`) — 8501 점유 해제하려면 kill
- `/tmp/dogfood-venv` — local dogfood install 잔존, 디스크만 차지

**다음 세션 첫 명령** (Phase 3-Web T49 진입):
```bash
cd /Users/jerome/AX/showcase && claude
/mem-resume
git log --oneline -10                          # HEAD 70327dd (T48.3) 확인
uv run pytest -q                               # 678 passed 재확인
grep -n "^### T49" specs/2026-05-08-phase3-plan-v2.md  # T49 spec (line 2394~)
```

**T49 핵심**: progress adapter (Streamlit 버전, T40 패턴 응용) + `as_display()` 강제 결과 카드 (현 `st.json` 단순 dump → 풍부한 UI: output_files 다운로드, metrics, failures expander, summary_text markdown) + TTL hook (`cleanup_expired_runs(ttl_hours=24)` Streamlit 진입 시점 결합). AC: `as_display` 외 raw `result` 가 `st.*` 위젯에 전달 차단 (R1-C1 single sanitizer).

---

## 16:00~ — Phase 3-Web 마감 + close audit (T49~T52, 6 commits + push)

**오후 후반 세션 누적**:
- `561d07f` **T49** feat(web) — `web/_progress.py` Streamlit adapter (모듈만 출시, plumb defer) + `web/_render.py` `render_result` 단일 sanitizer (R1-C1) + `runs/.gitignore` + TTL start hook (`cleanup_expired_runs(ttl_hours=24)`). Spec deviation 5건 disclose (ProgressEvent attribute access → dict, output_files zip-매칭, dead code 제거, Mapping 시그니처, 시각 검증 T50 흡수).
- `c197012` **T50** test(web) — `tests/test_streamlit_smoke.py` 5 tests (app import / run dir 격리 / **R1-C1 sentinel `ya29.STREAMLIT_RENDER_LEAK_TEST` leak 0 검증** / safe-mode 라운드트립 / 10-case schema 커버리지). dogfood smoke 3.11/3.12/3.13 cross-version local 재실행. Spec deviation: `case01` → `case09` (hermetic).
- `b9a1b50` **T51.5-fix(web)** — `_TOTAL_UPLOAD_CAP_BYTES` fail-early. `stream_save(*, remaining_total)` 추가 + partial-file unlink + 신규 test. R1-H2 흡수.
- `52cc3f7` **T51.5-fix(dogfood)** — SECRET_ENV_NAMES 14 → 21 (Anthropic / GitHub / GH / AWS×2 / HF / Slack 보강). R1-M5 흡수.
- `0f36a7a` **T51** test(audit) — 3-subagent 병렬 dispatch (security-engineer / system-architect / general-purpose), read-only audit. spec prompt verbatim. `specs/2026-05-12-phase3-audit.md` (180 lines). 종합 verdict: R1 GO-with-conditions / R2 GO-with-conditions (critical 1) / R3 Grade **A−**.
- `(T52 commit, 본 commit)` **T52** docs — design v2.1 §4.2-RETRACT framing (R2-C1) + README / CLAUDE / MEMORY 갱신 + promise.md Phase 3 close row + Phase 4 backlog 정리 + swap 비용 정정 표 (~1주 → ~1.5주).

**T51 audit 핵심 finding 처리**:
- **R2-C1** (Backends DI `_ =` 폐기, design §4.2 약속 vs 실 구현 정합 미실현) → T52 design retract framing 으로 흡수 + T-PHASE4-DI-1 backlog 명시화 (option a code rewire ~2d 대비 option b retract 가 Phase 3 의도와 비례).
- **R1-H1** false-positive (`runs/.gitignore` 가 이미 T49 fee9c97 에서 contents 전부 ignore) — audit doc 에 명시.
- **R1-H2** real bug, T51.5-fix(web) 즉시 close.
- **R1-M5** cheap coverage, T51.5-fix(dogfood) 즉시 close.
- 나머지 (R1 medium 5건 + low 8건 / R2 high 2건 + medium 3건 + low 3건 / R3 medium 2건) → Phase 4 backlog README.md 명시.

**Phase 3 종료 baseline (T52 docs commit)**:
- pytest: **684 passed, 4 skipped** (Phase 2 539 → +145 Phase 3 누적, 회귀 0)
- mypy --strict source: **73 files clean** (`packages/.../src + runner + cases + web`)
- mypy --strict tests/: **103 errors / 13 files** (T41.5 ceiling 유지)
- ruff: clean / dogfood smoke 3.11/3.12/3.13: OK
- 시연: 10/10
- CI matrix: macos-latest × Python 3.11/3.12/3.13 + dogfood `env -i` smoke + SECRET_ENV_NAMES 21건 leak guard
- 외부 사용 게이트: (1) 0/1 미충족 보존, (2) dogfood ✅ — 라벨 "import-ready 패키지"

**Phase 3 전체 close (Phase 3-A + Pkg + Web + audit + close docs)** — 8 commit 트랙으로 정리:
- T34.5 gate / T35~T41 refactor (8 commits) — Phase 3-A
- T41.5 ceiling / T41.6 boundary lock (2 commits) — 정합
- T42~T46 packaging (6 commits) — Phase 3-Pkg
- T47~T48.3 Streamlit MVP (7 commits) — Phase 3-Web 1단계
- T49~T50 progress + smoke (2 commits) — Phase 3-Web 마감
- T51 + T51.5 ×2 audit (3 commits) — close audit
- T52 docs (본 commit) — close

**핵심 교훈 (lessons.md 추가)**:
- 3-subagent 병렬 audit 가 critical 1건 (Backends DI 약속 vs 실 구현 정합) 을 catch — self-audit 1회로 끝나지 않음 (design v2.1 §0.0 의 self-revise 한계 lesson 재확인). reviewer 분리가 framing 거짓 catch 의 핵심.
- design retract framing 은 약속의 *철회* 가 아니라 *완성도 정직 표기* — Phase 4 backlog 명시화로 swap-ability 의도는 보존, 실 라우팅 migration 만 deferred. 비례 큰 code rewire 보다 정직 docs commit 이 close 의도와 정합.

**다음 세션 진입** (Phase 4 또는 외부 시연):
```bash
cd /Users/jerome/AX/showcase && claude
/mem-resume
git log --oneline -10                          # HEAD T52 docs 확인
uv run pytest -q                               # 684 passed, 4 skipped
cat specs/2026-05-12-phase3-audit.md           # close audit findings ★
cat README.md                                   # Phase 4 backlog ★
# 권장 entry task: T-PHASE4-DI-1 (cases 라우팅 backends.* 전환, ~2d)
```
