"""Internal helpers — not part of the public surface.

Modules under this package are implementation details. External callers must not
import from ``flowcoder_office_tools._internal.*``; doing so is unsupported and
breaks without notice. Public API lives in ``flowcoder_office_tools.{excel,
messaging, docgen, ocr, ai, common, backends, protocols}`` only.
"""
